/**
 * Canvas 配置常量集中管理，方便复用与调优。
 */
export const NODE_WIDTH = 180;
export const NODE_HEIGHT = 100;

/** handle 圆心相对节点边缘的偏移（px） */
export const HANDLE_OFFSET = 6;

/** 边可视宽度与命中宽度 */
export const EDGE_STROKE_WIDTH = 2;
export const EDGE_SELECTED_STROKE_WIDTH = 3;
export const EDGE_HIT_STROKE_WIDTH = 12;

/** 背景网格基准 */
export const GRID_DOT_SPACING_BASE = 20;
export const GRID_DOT_RADIUS_BASE = 1;

/** 默认 viewport */
export const DEFAULT_VIEWPORT = { x: 400, y: 300, zoom: 1, z: 1 };


